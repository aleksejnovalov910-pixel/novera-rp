# NOVERA v0.18 is done when

A normal player can connect to the public host, create an account and character, play a starter gameplay loop, own/use basic assets, disconnect, reconnect and continue with the same persistent state — without administrator intervention and without runtime error popups.

Until that is true, the build remains `-dev`.
